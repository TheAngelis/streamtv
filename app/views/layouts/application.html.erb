<!DOCTYPE html>
<html>
<head>
  <title>Streamsite</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <link href="assets/css/bootstrap-responsive.css" rel="stylesheet">
  <link href="css/bootstrap.min.css" rel="stylesheet" media="screen">
  <script src="http://code.jquery.com/jquery.js"></script>
  <script src="js/bootstrap.min.js"></script>

    <style type="text/css">
      body {
        padding-top: 20px;
        padding-bottom: 20px;
        background-color: #FcFcFc;

      }
.container-fluid {
     padding-right: 0;
     padding-left: 0;
     margin-right: -30px;
     margin-left: -30px;
   }

   .no-padding{
     padding: auto;
     margin: auto;

   }
   .padding-page{
     padding-left: 50px;
     padding-right: 50px;

   }

      iframe {
  max-width: 100%;
}

a:link, a:visited {
  text-decoration: none;
  color: #4D3270;
	}
a:hover {
	text-decoration: none;
	color: #4D3270;
	}
a:active {
	text-decoration: none
	}

  .nav-pills > .active > a, .nav-pills > .active > a:hover {
    background-color: #4D3270;
// This changes the color of the active
}
.nav-pills>li.active>a:hover {
    background-color: #4D3270;
}
// This changes the color of the hovered pill



      #beta {
       margin: 10px;
               }
      .titlevideo{
        color: #BF0410;

               }


      .emailsignin{
        color: #121212;

               }

               .uplinepage{
                 margin-top: -30px;

               }
      .bgdark{
        padding-top: 30px;
        padding-bottom: 30px;
        background: #24202B url("/assets/bgimg.jpg");
        background-repeat: repeat-x;
        color: #CCCCCC;

      }

      .textarea-description { width: 550px; height: 75px; }
      .textarea-code { width: 550px; height: 95px; }

      .form-signin {
        max-width: 350px;
        padding: 19px 29px 29px;
        margin: 0 auto 20px;
        background-color: #F5F5F5;
        border: 1px solid #E5E5E5;
        -webkit-border-radius: 5px;
           -moz-border-radius: 5px;
                border-radius: 5px;
        -webkit-box-shadow: 0 1px 2px rgba(0,0,0,.05);
           -moz-box-shadow: 0 1px 2px rgba(0,0,0,.05);
                box-shadow: 0 1px 2px rgba(0,0,0,.05);
      }
      .form-signin .form-signin-heading,
      .form-signin .checkbox {
        margin-bottom: 10px;
      }
      .form-signin input[type="text"],
      .form-signin input[type="password"] {
        font-size: 16px;
        height: 20px;
        margin-bottom: 15px;
        padding: 7px 9px;
      }
  .user-show-admin {
          max-width: 600px;
          padding: 19px 29px 29px;
          margin: 0 auto 20px;
          background-color: #fff;
          border: 1px solid #e5e5e5;
          -webkit-border-radius: 5px;
             -moz-border-radius: 5px;
                  border-radius: 5px;
          -webkit-box-shadow: 0 1px 2px rgba(0,0,0,.05);
             -moz-box-shadow: 0 1px 2px rgba(0,0,0,.05);
                  box-shadow: 0 1px 2px rgba(0,0,0,.05);
        }

      .video-edit {
          max-width: 300px;
        padding: 10px 10px 10px;

        background-color: #F5F5F5;
        border: 1px solid #E5E5E5;
        -webkit-border-radius: 5px;
           -moz-border-radius: 5px;
                border-radius: 5px;
        -webkit-box-shadow: 0 1px 2px rgba(0,0,0,.05);
           -moz-box-shadow: 0 1px 2px rgba(0,0,0,.05);
                box-shadow: 0 1px 2px rgba(0,0,0,.05);
      }


.account-form {
          max-width: 500px;
          padding: 19px 29px 29px;
          margin: 0 auto 20px;
          background-color: #fff;
          border: 1px solid #e5e5e5;
          -webkit-border-radius: 5px;
             -moz-border-radius: 5px;
                  border-radius: 5px;
          -webkit-box-shadow: 0 1px 2px rgba(0,0,0,.05);
             -moz-box-shadow: 0 1px 2px rgba(0,0,0,.05);
                  box-shadow: 0 1px 2px rgba(0,0,0,.05);
        }


    </style>
</head>
<body>
  <div class="container-fluid padding-page">
    <div class="row-fluid">


         <%= link_to(image_tag("logo.png"), root_path, { :class=>"titlevideo" }) %>

           <ul class="nav nav-pills pull-right">

        <% if user_signed_in? %>

          <!-- EMAIL -->
          <li><a class="emailsignin"><strong><%= current_user.email %></strong></a> </li>

          <!-- ADMIN -->
          <% if current_user.admin? %>
               <li class="<% if current_page?(admin_index_path) %> active<% end %>"><%= link_to "Admin", admin_index_path %></li>
          <% end %>

          <!-- DASHBOARD -->
          <li class="<% if current_page?(dashboard_index_path) %> active<% end %><% if current_page?(dashboard_audio_path) %> active<% end %>"><%= link_to "Dashboard", root_path %></li>

          <!-- MY ACCOUNT -->
          <li class="<% if current_page?(edit_user_registration_path) %> active<% end %>"><%= link_to "My Account", edit_user_registration_path %></li>

          <!-- SIGN OUT -->
          <li> <%= link_to "Sign out", destroy_user_session_path, :method => :delete %></li>


        <% else %>
          <li class="active"><%= link_to "Home", root_path %></li>
          <li><%= link_to "Register", new_user_registration_path %></li>
          <li><%= link_to "Sign in", new_user_session_path %></li>
        <% end %>
      </ul>
    </div>
  </div>

      <hr>


  <div class="container-fluid">


    <%= yield %>

  </div>
  <hr>
</body>
</html>
